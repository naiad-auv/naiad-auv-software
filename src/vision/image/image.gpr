with "../vision.gpr";

project Image is

   for Source_Dirs use ("src");
   for Object_Dir use "obj/";
   for Main use ();

   package Make is
      for Makefile use "Makefile";
   end Make;

end Image;

