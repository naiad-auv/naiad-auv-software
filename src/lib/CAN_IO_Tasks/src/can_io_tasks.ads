-- USAGE:
-- Add this project as a dependency to your project,
-- then add "with CAN_IO_Tasks.Tasks;" to your package spec.
-- This will start the CAN IO tasks.

package CAN_IO_Tasks is
end CAN_IO_Tasks;
